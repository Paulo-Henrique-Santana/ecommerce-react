import styled from "styled-components";

export const Ul = styled.ul`
  li {
    display: flex;
    align-items: center;
    gap: 5px;
    margin-bottom: 7px;
    width: max-content;
    cursor: pointer;
  }
  li span {
    margin-top: 1px;
  }
`;
